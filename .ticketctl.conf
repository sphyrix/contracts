# Which ticket record this project uses (ticketctl / implementing-tickets).
#
# COMMITTED ON PURPOSE. Which tracker a project uses is a project decision, so it must
# survive a fresh clone. An individual clone can override with .orchestrator/backend.conf.
BACKEND=github
REPO=sphyrix/contracts
